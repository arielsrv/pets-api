package main

import (
	"log"
	"gitlab.com/iskaypet/ikp_go-secret"
)

func main() {
	secretStore := secretStore.NewSecretStore()
	secret := secretStore.GetSecret("$PETS_APPNAME_SECRETKEY")
	if value.Err != nil {
		log.Fatal(err)
	}
	log.Print(secret.Value)
}
